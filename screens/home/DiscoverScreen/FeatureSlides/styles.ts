import styled from 'styled-components/native'
import { ResponsiveImageBackground } from '@components'

export const Background = styled(ResponsiveImageBackground)`
  flex: 1;
  resize-mode: cover;
  width: 100%;
`
